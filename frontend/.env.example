# ===================================
# Frontend Environment Variables
# ===================================

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:8000"

# Production example:
# NEXT_PUBLIC_API_URL="https://api.yourdomain.com"

# ===================================
# Production Recommendations
# ===================================

# 1. API URL should point to your production backend
#    Example: https://api.inheritance-calculator.com

# 2. For Next.js deployment platforms:
#    - Vercel: Set in project settings
#    - Netlify: Set in build environment
#    - Custom server: Set in .env.production

# 3. NEXT_PUBLIC_ prefix makes variables available to browser
#    - Only use for non-sensitive data
#    - Never expose API keys or secrets

# 4. Build-time vs Runtime:
#    - NEXT_PUBLIC_ variables are embedded at build time
#    - Update requires rebuild if changed
